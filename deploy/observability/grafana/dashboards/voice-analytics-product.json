{
  "uid": "voice-analytics-product",
  "title": "Voice Analytics — Product",
  "tags": ["voice", "analytics", "product"],
  "timezone": "browser",
  "schemaVersion": 39,
  "version": 1,
  "refresh": "1m",
  "time": { "from": "now-7d", "to": "now" },
  "panels": [
    {
      "id": 1,
      "type": "timeseries",
      "title": "DAU (unique users / day)",
      "gridPos": { "h": 8, "w": 12, "x": 0, "y": 0 },
      "datasource": { "type": "grafana-clickhouse-datasource", "uid": "clickhouse" },
      "targets": [
        {
          "refId": "A",
          "queryType": "sql",
          "rawSql": "SELECT toDate(timestamp) AS date, uniqExact(user_id_hashed) AS dau FROM voice.events_logical WHERE user_id_hashed != '' GROUP BY date ORDER BY date",
          "format": 1
        }
      ]
    },
    {
      "id": 2,
      "type": "timeseries",
      "title": "Events per type (daily)",
      "gridPos": { "h": 8, "w": 12, "x": 12, "y": 0 },
      "datasource": { "type": "grafana-clickhouse-datasource", "uid": "clickhouse" },
      "targets": [
        {
          "refId": "A",
          "queryType": "sql",
          "rawSql": "SELECT toDate(timestamp) AS date, event_type, count() AS cnt FROM voice.events_logical GROUP BY date, event_type ORDER BY date",
          "format": 1
        }
      ]
    },
    {
      "id": 3,
      "type": "stat",
      "title": "Registrations (profile_created / day)",
      "gridPos": { "h": 4, "w": 8, "x": 0, "y": 8 },
      "datasource": { "type": "grafana-clickhouse-datasource", "uid": "clickhouse" },
      "targets": [
        {
          "refId": "A",
          "queryType": "sql",
          "rawSql": "SELECT count() FROM voice.events_logical WHERE event_type = 'profile_created' AND toDate(timestamp) = today()",
          "format": 1
        }
      ]
    }
  ]
}
