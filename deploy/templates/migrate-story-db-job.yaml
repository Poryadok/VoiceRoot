# One-shot golang-migrate Job for story_db.
# Substitute __K_NAMESPACE__, __MIGRATE_IMAGE_TAG__ (default migrate/migrate:v4.18.1).
---
apiVersion: batch/v1
kind: Job
metadata:
  name: voice-migrate-story-db
  namespace: __K_NAMESPACE__
  labels:
    app.kubernetes.io/name: voice-migrate-story-db
    app.kubernetes.io/component: migration
spec:
  ttlSecondsAfterFinished: 86400
  backoffLimit: 2
  activeDeadlineSeconds: 180
  template:
    metadata:
      labels:
        app.kubernetes.io/name: voice-migrate-story-db
    spec:
      restartPolicy: Never
      containers:
        - name: migrate
          image: migrate/migrate:__MIGRATE_IMAGE_TAG__
          args:
            - -path=/migrations
            - -database=__DATABASE_URL__
            - up
          volumeMounts:
            - name: migrations
              mountPath: /migrations
              readOnly: true
      volumes:
        - name: migrations
          configMap:
            name: voice-story-db-migrations
            optional: false
