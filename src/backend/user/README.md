# User Service

Go microservice: profiles, settings, privacy, verification, avatar presign (R2). gRPC `UserService`. PostgreSQL `user_db`, Redis cache. Gateway `/api/v1/users/**`. See `docs/microservices/user-service.md`.
